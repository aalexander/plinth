name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  # Shared deterministic floor (secrets, SAST, dependency scan) lives in Plinth.
  # Pinned to a Plinth release tag — bump deliberately. Replace OWNER.
  floor:
    uses: OWNER/plinth/.github/workflows/plinth-floor.yml@v3

  # Project-specific checks. Fill in YOUR stack's commands; this part is yours.
  checks:
    runs-on: ubuntu-latest   # macos-latest for Apple-Silicon parity
    steps:
      - uses: actions/checkout@v4
      # --- Rust ---
      # - run: cargo fmt --check
      # - run: cargo clippy -- -D warnings
      # - run: cargo test
      # - run: cargo build --release
      # --- Node ---
      # - uses: actions/setup-node@v4
      #   with: { node-version: '24' }
      # - run: npm ci
      # - run: npm run typecheck
      # - run: npm run lint
      # - run: npm test
      # - run: npm run build
      - run: echo "Add your stack's commands above, then delete this line."
