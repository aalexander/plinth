# Residual triage (v5.0.5 closeout — 2026-07-29)

Source: bound residual after PR #57 residual land (~51 findings). Disposition
after product fixes in 5.0.5 (including review rounds).

## Disposition summary

| Class | Action |
|-------|--------|
| Stale dual-pass / pre-fix claims | **DROP** |
| Verify corrupt prior / re-carry | **FIXED** |
| Sticky test-gap classifier drift | **FIXED** |
| Thrash demotes real bugs outside delta | **FIXED** (thrash-class only) |
| VERSION / SIGPIPE floors | **FIXED** |
| agy audit (`-p` + stdin) | **FIXED** |
| Plan hollow seats (markdown + JSON) | **FIXED** (3 sections; string-only arrays + string verdict) |
| DRIVER-project scaffold vs custom comments | **FIXED** (stock fingerprint; unicode kept) |
| Phase env cannot downgrade harden | **FIXED** |
| before-sha transcript self-report | **FIXED** |
| `.plinth-version` pin vs product | **POLICY** — dogfood pin trails release |

## Residual file state

Residual is **unbound** with empty findings after 5.0.5 closeout.
